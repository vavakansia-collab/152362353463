# AnonAsk Bot

Телеграм-бот для анонимных вопросов. Пользователь генерирует персональную ссылку, размещает её в профиле или соцсетях, а любой желающий может перейти и прислать сообщение. Получатель видит входящие вопросы и отвечает на них прямо в чате бота — никаких платных функций и донатов.

---

## Быстрый старт

1. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

2. **Установите токен** в `config.py`, присвоив значение переменной `BOT_TOKEN` (по умолчанию уже прописан тестовый токен).

3. **Запустите бота**
   ```bash
   python main.py
   ```

База `database.db` и лог-файлы в `logs/` создаются автоматически.

---

## Архитектура и основные модули

| Файл/папка | Роль |
|------------|------|
| `main.py` | Точка входа: инициализация БД, включение роутеров (команды, inline-кнопки, обработчики ошибок) и запуск поллинга `aiogram` 3. |
| `createbot.py` | Создаёт экземпляры `Bot`, `Dispatcher`, `MemoryStorage`. Вынесено в отдельный модуль, чтобы единообразно импортировать `bot`/`dp`. |
| `config.py` | Хранит константы `BOT_TOKEN` и `LOG_CHAT_ID`, используемые ботом и логированием. |
| `commands.py` | `/start` без аргументов — регистрация и выдача меню. `/start <payload>` — deep-link сценарий для отправки вопроса конкретному пользователю. Также содержит FSM `Form.question` для ввода текста и логику пересылки ответов между собеседниками. |
| `inlinekeyboard.py` | Обработка inline-кнопки для генерации личной ссылки `t.me/<bot>?start=...`. |
| `errors.py` | Глобальный роутер для логирования/ответов на ошибки (подключается в `main.py`). |
| `inline_keyboards.py` | Конструктор InlineKeyboardMarkup с кнопкой генерации ссылки. |
| `sqlite_db.py` | Подключение к SQLite (`database.db` в корне проекта), создание таблиц (`Users`, `Messages`), функции записи пользователей и сообщений. |
| `loggers.py` | Настраивает три именованных логгера (`database_logger`, `handlers_logger`, `main_logger`) с выводом в файлы `logs/*.log` и консоль. |

Диаграмма данных (упрощённо):

- `Users(user_id PRIMARY KEY, username, register_date)`
- `Messages(message_id PRIMARY KEY, recipient, sender, content, register_date)`

---

## Сценарии работы

### 1. Регистрация и генерация ссылки
- `/start` → бот добавляет пользователя в таблицу `Users`, приветствует и показывает главное меню с кнопкой «🔗 Ссылка для анонимных вопросов».
- Кнопка «Ссылка для анонимных вопросов» вызывает `create_start_link()` и отдаёт готовый deeplink, который можно разместить в описании профиля или соцсетях.

### 2. Получение анонимного вопроса
- Отправитель переходит по ссылке вида `https://t.me/<bot>?start=<payload>`. Параметр декодируется, и бот понимает, кому адресовать сообщение.
- FSM `Form.question` принимает одно текстовое сообщение, сохраняет его в `Messages` и пересылает получателю от имени бота.
- Для ответа достаточно свайпнуть (Reply) на сообщение бота; цепочка sender ↔ recipient поддерживается внутренним реестром маршрутов, который сопоставляет `message_id` с парой собеседников.

### 3. Повторные ответы на вопросы
- Чтобы продолжить переписку, получатель или отправитель отвечает свайпом (Reply) на сообщение бота. Хендлеры отслеживают цепочку и пересылают новые реплики адресату.

---

## Логи и диагностика

- Все действия фиксируются в `logs/database.log`, `logs/handlers.log`, `logs/main.log`.
- Для расширенной диагностики можно заменить `MemoryStorage` на Redis/Beta в `createbot.py` и добавить middleware.

---

## Планы на развитие
- Перенести реестр маршрутов из оперативной памяти в постоянное хранилище (например, Redis или SQL), чтобы не терять цепочки при рестарте бота.
- Добавить webhooks и управление конфигурацией через переменные окружения.
- Добавить расширенную статистику: количество вопросов, активность по пользователям.

На текущий момент репозиторий покрывает базовый сценарий «ссылка → вопрос → ответ» без каких-либо платных функций.
